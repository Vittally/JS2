class validate {
    constructor(field, regexp){
        this.field = field;
        this.regexp = regexp;
    }
    check(field){
        regexp.test(field){
            alert('Все ок')
        }
    }

}